import{notFound}from"next/navigation";import{DeliveryTable}from"@/components/delivery-table";import{listDeliveries}from"@/lib/store";export const dynamic="force-dynamic";const valid=["analytics","advertising","crm"];export default async function Page({params}:{params:Promise<{receiver:string}>}){const{receiver}=await params;if(!valid.includes(receiver))notFound();const deliveries=await listDeliveries(receiver);return <main><div className="page-head"><div><p>Receiver</p><h1>{receiver[0].toUpperCase()+receiver.slice(1)}</h1><span>POST /ingest/{receiver} · signed gateway requests only</span></div><strong>{deliveries.length} accepted</strong></div><section className="proof"><h2>Canonical field proof</h2><div><span>Expected approved fields</span><code>order.id · order.value · product.id · product.category</code></div><div><span>Forbidden field</span><code className="blocked">customer.phone</code></div></section><section className="panel"><div className="panel-head"><h2>Accepted deliveries</h2><span>Direct unsigned requests are rejected</span></div><DeliveryTable deliveries={deliveries}/></section></main>}
