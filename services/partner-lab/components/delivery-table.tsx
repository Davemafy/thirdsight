import Link from"next/link";import type{Delivery}from"@/lib/store";export function DeliveryTable({deliveries}:{deliveries:Delivery[]}){if(!deliveries.length)return <div className="empty"><h2>No deliveries yet</h2><p>Signed gateway events will appear here after they are accepted.</p></div>;return <div className="table"><div className="thead"><span>Received</span><span>Event</span><span>Integration</span><span>Fields</span><span>Signature</span><span/></div>{deliveries.map(d=><div className="trow" key={d.id}><time>{new Date(d.receivedAt).toLocaleString("en-NG")}</time><strong>{d.eventName}</strong><code>{d.integrationId}</code><span>{d.receivedFields.length}</span><span className="verified">Verified</span><Link href={`/deliveries/${d.id}`}>Inspect</Link></div>)}</div>}
