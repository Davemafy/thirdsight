import{describe,expect,it}from"vitest";import{calculateCart,formatNaira}from"../../lib/money";describe("cart prices",()=>{it("calculates line totals, discount and free delivery",()=>expect(calculateCart({lines:[{price:599000,quantity:2}],promoPercent:10})).toEqual({subtotal:1198000,discount:119800,delivery:0,total:1078200}));it("charges delivery below threshold",()=>expect(calculateCart({lines:[{price:39000,quantity:1}]}).total).toBe(46500));it("formats naira",()=>expect(formatNaira(599000)).toContain("599,000"))});
