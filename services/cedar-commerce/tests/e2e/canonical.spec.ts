import{test,expect}from"@playwright/test";test("search, configure, cart and synthetic guest checkout",async({page})=>{await page.goto("/");await page.getByLabel("Search products").fill("Arc One");await page.getByLabel("Search products").press("Enter");await expect(page).toHaveURL(/search\?q=Arc\+One/);await page.getByRole("link",{name:"CEDAR Arc One"}).first().click();await expect(page).toHaveURL(/products\/cedar-arc-one/);await page.getByRole("button",{name:"256GB"}).click();await page.getByRole("button",{name:/Add to cart/}).click();await page.getByRole("link",{name:/Cart/}).click();await page.getByRole("button",{name:"Increase quantity"}).click();await expect(page.getByText("2",{exact:true})).toBeVisible();await page.getByRole("link",{name:"Proceed to checkout"}).click();await page.getByLabel("Email").fill("ada.okafor@example.test");await page.getByLabel("Synthetic phone").fill("+2348000000000");await page.getByLabel("First name").fill("Ada");await page.getByLabel("Last name").fill("Okafor");await page.getByRole("button",{name:"Continue"}).click();await page.getByLabel("Address").fill("12 Demo Crescent");await page.getByLabel("City").fill("Abuja");await page.getByRole("button",{name:"Continue"}).click();await page.getByRole("button",{name:"Continue"}).click();await page.getByRole("button",{name:"Place synthetic order"}).click();await expect(page).toHaveURL(/orders\/.+\/confirmation/);await expect(page.getByText("Order confirmed")).toBeVisible()});
