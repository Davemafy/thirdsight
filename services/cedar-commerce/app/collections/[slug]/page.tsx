import{notFound}from"next/navigation";import{StoreHeader}from"@/components/store-header";import{StoreFooter}from"@/components/store-footer";import{ProductCard}from"@/components/product-card";import{getCollection,listProducts}from"@/lib/commerce-store";
const categories=new Set(["phones","headphones","earbuds","smartwatches","speakers","keyboards","cameras","power"]);
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const collection=categories.has(slug)?{name:slug[0].toUpperCase()+slug.slice(1),description:`Current ${slug} with live prices, finishes and availability.`,products:(await listProducts({category:slug,limit:24})).items}:await getCollection(slug);if(!collection)notFound();return <><StoreHeader/><main className="collection-page"><p className="breadcrumbs">Home / Collections / {collection.name}</p><div className="collection-head"><h1>{collection.name}</h1><p>{collection.description}</p><span>{collection.products.length} products</span></div><div className="product-grid">{collection.products.map(p=><ProductCard key={p.id} product={p}/>)}</div></main><StoreFooter/></>}
