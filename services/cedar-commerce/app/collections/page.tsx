import Link from"next/link";import{StoreHeader}from"@/components/store-header";import{StoreFooter}from"@/components/store-footer";import{listCollections}from"@/lib/commerce-store";export default async function Page(){const collections=await listCollections();return <><StoreHeader/><main className="content-page"><p className="breadcrumbs">Home / Collections</p><h1>Collections</h1><p>Shop focused groups for mobile, audio, work and new arrivals.</p><div className="collection-list">{collections.map(c=><Link key={c.slug} href={`/collections/${c.slug}`}><h2>{c.name}</h2><p>{c.description}</p><span>{c.productSlugs.length} products</span></Link>)}</div></main><StoreFooter/></>}
