import{StoreHeader}from"@/components/store-header";import{StoreFooter}from"@/components/store-footer";import{listProducts}from"@/lib/commerce-store";import{formatNaira}from"@/lib/money";export default async function Page(){const{items}=await listProducts({category:"phones",limit:3});return <><StoreHeader/><main className="content-page"><p className="breadcrumbs">Home / Compare</p><h1>Compare phones</h1><p>Key differences across current compatible models.</p><div className="compare-table"><div><strong>Model</strong>{items.map(p=><b key={p.id}>{p.name}</b>)}</div><div><strong>From</strong>{items.map(p=><span key={p.id}>{formatNaira(p.variants[0].price)}</span>)}</div>{["Display","Camera","Battery","Protection"].map(k=><div key={k}><strong>{k}</strong>{items.map(p=><span key={p.id}>{p.specifications[k]??"—"}</span>)}</div>)}</div></main><StoreFooter/></>}
