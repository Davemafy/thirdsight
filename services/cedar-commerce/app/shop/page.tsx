import Link from"next/link";import{StoreHeader}from"@/components/store-header";import{StoreFooter}from"@/components/store-footer";import{ProductCard}from"@/components/product-card";import{listProducts}from"@/lib/commerce-store";
export default async function Shop({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const p=await searchParams;const result=await listProducts({category:p.category,q:p.q,sort:p.sort,inStock:p.stock==="in",minPrice:p.min?Number(p.min):undefined,maxPrice:p.max?Number(p.max):undefined,page:p.page?Number(p.page):1,limit:9});const active=[["Category",p.category],["Minimum",p.min&&`₦${Number(p.min).toLocaleString("en-NG")}`],["Maximum",p.max&&`₦${Number(p.max).toLocaleString("en-NG")}`],["Stock",p.stock==="in"?"In stock":undefined]].filter((x):x is string[]=>Boolean(x[1]));const pageHref=(page:number)=>`/shop?${new URLSearchParams(Object.entries({...p,page:String(page)}).filter((x):x is[string,string]=>Boolean(x[1]))).toString()}`;return <><StoreHeader/><main className="listing-shell"><aside className="filters"><form><h2>Filters</h2><label>Category<select name="category" defaultValue={p.category??""}><option value="">All categories</option>{["phones","headphones","earbuds","smartwatches","speakers","keyboards","cameras","power"].map(v=><option key={v}>{v}</option>)}</select></label><label>Minimum price<input name="min" type="number" defaultValue={p.min}/></label><label>Maximum price<input name="max" type="number" defaultValue={p.max}/></label><label className="check"><input name="stock" value="in" type="checkbox" defaultChecked={p.stock==="in"}/> In stock only</label><button>Apply filters</button></form></aside><section className="results"><div className="results-head"><div><p className="breadcrumbs">Home / Shop</p><h1>Shop technology</h1><p>{result.total} products, with current prices and availability.</p></div><form><label>Sort by<select name="sort" defaultValue={p.sort??"featured"}><option value="featured">Featured</option><option value="price-asc">Price: low to high</option><option value="price-desc">Price: high to low</option><option value="rating">Highest rated</option></select></label>{p.category&&<input type="hidden" name="category" value={p.category}/>}<button>Sort</button></form></div>{active.length>0&&<div className="active-filters" aria-label="Active filters">{active.map(([label,value])=><span key={label}>{label}: {value}</span>)}<Link href="/shop">Clear all</Link></div>}{result.items.length?<><div className="product-grid listing-grid">{result.items.map(x=><ProductCard key={x.id} product={x}/>)}</div>{result.pages>1&&<nav className="pagination" aria-label="Product pages">{result.page>1&&<Link href={pageHref(result.page-1)}>Previous</Link>}<span>Page {result.page} of {result.pages}</span>{result.page<result.pages&&<Link href={pageHref(result.page+1)}>Next</Link>}</nav>}</>:<div className="empty-state"><h2>No products match</h2><p>Clear a filter or try a broader search.</p><Link href="/shop">Clear filters</Link></div>}</section></main><StoreFooter/></>}
