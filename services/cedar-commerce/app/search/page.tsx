import Link from"next/link";import{StoreHeader}from"@/components/store-header";import{StoreFooter}from"@/components/store-footer";import{ProductCard}from"@/components/product-card";import{listProducts}from"@/lib/commerce-store";
export default async function Page({searchParams}:{searchParams:Promise<{q?:string}>}){const q=(await searchParams).q??"";const r=await listProducts({q,limit:24});return <><StoreHeader/><main className="collection-page"><p className="breadcrumbs">Home / Search</p><div className="collection-head"><h1>{q?`Results for “${q}”`:"Search CEDAR"}</h1><p>{r.total} matching products</p></div>{r.items.length?<div className="product-grid">{r.items.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="empty-state"><h2>No matches found</h2><p>Try a category or a shorter product name.</p><Link href="/shop">Browse all products</Link></div>}</main><StoreFooter/></>}
