import Link from"next/link";import{notFound}from"next/navigation";import{StoreHeader}from"@/components/store-header";import{StoreFooter}from"@/components/store-footer";import{ProductGallery}from"@/components/product-gallery";import{ProductPurchase}from"@/components/product-purchase";import{ProductCard}from"@/components/product-card";import{getProduct}from"@/lib/commerce-store";
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const product=await getProduct(slug);if(!product)notFound();const related=(await Promise.all(product.relatedSlugs.map(getProduct))).filter(Boolean);return <><StoreHeader/><main className="pdp"><nav className="breadcrumbs"><Link href="/">Home</Link> / <Link href={`/collections/${product.category}`}>{product.category}</Link> / {product.name}</nav><section className="pdp-main"><ProductGallery images={product.images} name={product.name}/><ProductPurchase product={product}/></section><section className="specs"><div><p className="eyebrow">Product details</p><h2>Key specifications</h2><p>{product.description}</p></div><dl>{Object.entries(product.specifications).map(([k,v])=><div key={k}><dt>{k}</dt><dd>{v}</dd></div>)}</dl></section><section className="delivery-copy"><div><h2>Delivery and returns</h2><p>{product.deliveryEstimate}. Delivery dates are confirmed at checkout. Unused products can be returned within 14 days.</p></div><div><h2>Warranty</h2><p>{product.warranty}. Keep your synthetic order number for support.</p></div></section><section id="reviews" className="reviews"><h2>Customer reviews</h2><strong>{product.rating} out of 5</strong><p>Based on {product.reviewCount} synthetic demonstration reviews.</p></section>{related.length>0&&<section className="catalog-section"><div className="section-heading"><h2>Complete your setup</h2></div><div className="product-grid related">{related.map(p=><ProductCard key={p!.id} product={p!}/>)}</div></section>}</main><StoreFooter/></>}
