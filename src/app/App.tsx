import { useEffect, useState } from "react";
import { Activity, CircleAlert, Radio, ShieldCheck } from "lucide-react";
import type { EvidenceClaim, PurposeEvidence, BrowserCapabilityLowerBound, RuntimeAccessEvidence, BusinessContextEvidence } from "../domain/evidence";
type E={recordId:string;observedAt:string;integrationId:string|null;integrationResolution:string;should:EvidenceClaim<PurposeEvidence>;could:EvidenceClaim<BrowserCapabilityLowerBound>;did:EvidenceClaim<RuntimeAccessEvidence>;why:EvidenceClaim<BusinessContextEvidence>;outcome:"PREVENTED"|"DETECTED"|null};
export default function App(){const [events,setEvents]=useState<E[]>([]),[selected,setSelected]=useState(0);const [error,setError]=useState(false);useEffect(()=>{let live=true;const load=()=>fetch("/api/console-events",{cache:"no-store"}).then(r=>{if(!r.ok)throw 0;return r.json()}).then(d=>{if(live){setEvents(d.history??[]);setError(false)}}).catch(()=>live&&setError(true));load();const id=setInterval(load,5000);return()=>{live=false;clearInterval(id)}},[]);const e=events[selected]??events[0];return <div className="console-shell"><aside><div className="brand"><span className="brandmark"><ShieldCheck size={19}/></span><div><strong>ThirdSight</strong><small>Evidence console</small></div></div><nav><span>LIVE EVIDENCE</span>{events.map((x,i)=><button className={i===selected?"active":""} onClick={()=>setSelected(i)} key={x.recordId}><Activity size={14}/><div><strong>{x.integrationId??"Unresolved integration"}</strong><small>{new Date(x.observedAt).toLocaleTimeString()}</small></div></button>)}</nav></aside><main><header><div><span className="eyebrow">Continuous access verification</span><h1>What happened, and what can we prove?</h1><p>Every answer below comes from persisted ThirdSight evidence. Unknown stays unknown.</p></div><span className="live"><Radio size={14}/> Live</span></header>{error?<Empty text="Live evidence is unavailable. ThirdSight will not substitute mock data."/>:!e?<Empty text="Waiting for persisted evidence. No demonstration cards are generated."/>:<><section className="event-head"><div><span className="eyebrow">Evidence record</span><strong>{e.integrationId??"Identity unresolved"}</strong><small>{e.recordId}</small></div><Outcome value={e.outcome}/></section><section className="questions"><Claim title="Should?" claim={e.should}/><Claim title="Could?" claim={e.could}/><Claim title="Did?" claim={e.did}/><Claim title="Why?" claim={e.why}/></section><section className="action"><span className="eyebrow">What ThirdSight actually did</span>{e.outcome?<><h2>{e.outcome}</h2><p>{e.outcome==="PREVENTED"?"The unjustified access was constrained before transmission.":"Observed after access/transmission. This is detection, not prevention."}</p></>:<><h2>NO ENFORCEMENT OUTCOME RECORDED</h2><p>The stored evidence proves neither prevention nor post-transmission detection, so the console labels neither.</p></>}<div className="meta"><span>Identity <b>{e.integrationResolution}</b></span><span>Observed <b>{new Date(e.observedAt).toLocaleString()}</b></span></div></section></>}</main></div>}
function Claim({title,claim}:{title:string;claim:EvidenceClaim<unknown>}){return <article className="claim"><div><span className="question">{title}</span><span className={"status "+claim.status.toLowerCase()}>{claim.status}</span></div><pre>{claim.value?JSON.stringify(claim.value,null,2):"Unknown"}</pre><p>{claim.reason??"Evidence recorded without an explanatory note."}</p><footer><b>{claim.confidence}</b><span>{claim.provenance.map(p=>p.source).join(" · ")||"no provenance"}</span></footer></article>}
function Outcome({value}:{value:E["outcome"]}){return <span className={"outcome "+(value?.toLowerCase()??"unknown")}>{value??"UNRESOLVED OUTCOME"}</span>}
function Empty({text}:{text:string}){return <div className="empty"><CircleAlert/><strong>{text}</strong></div>}
