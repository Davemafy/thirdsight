import{describe,expect,it}from"vitest";import{gatewaySignature,parseGatewayInput,resolveRegisteredIntegration,verifyGatewaySignature}from"./gateway-policy.js";
const input={merchantId:"cedar-commerce",integrationId:"analytics-partner",purpose:"purchase-measurement",eventName:"checkout.completed",idempotencyKey:"evt_unique_value",occurredAt:"2026-09-21T10:00:00.000Z",fields:{"order.id":"ORD-1"},context:{source:"cedar-backend",environment:"synthetic-demo"}};
describe("gateway policy",()=>{it("accepts the fixed dispatch shape",()=>expect(parseGatewayInput(input)).not.toBeNull());it("rejects arbitrary flat fields",()=>expect(parseGatewayInput({...input,fields:{phone:"x"}})).toBeNull());it("validates HMAC and age",()=>{const t="1789984800000",secret="12345678901234567890123456789012",body=JSON.stringify(input),sig=gatewaySignature(secret,t,body);expect(verifyGatewaySignature(secret,t,body,sig,1789984800000)).toBe(true);expect(verifyGatewaySignature(secret,t,body,sig,1789985400001)).toBe(false)});it("rejects unregistered integrations",()=>expect(resolveRegisteredIntegration("shadow",{})).toBeNull())});
